schema:
  required_columns:
    - id
    - date
    - price
    - category

  data_types:
    id: integer
    date: date
    price: numeric
    category: text

range_rules:
  amount:
    min: 0
  quantity:
    min: 1
  order_date:
    not_future: true
